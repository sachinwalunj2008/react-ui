type MdashProps = {
    /** Optionally add a class */
    customClass?: string;
    /** Optionally use the dark-purple color variation */
    dark?: boolean;
};
export default function Mdash({ customClass, dark, }: MdashProps): JSX.Element;
export {};

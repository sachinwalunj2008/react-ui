declare const productData: {
    label: string;
    data: string;
    check: boolean;
}[], otherData: {
    label: string;
    data: string;
    check: boolean;
}[];
export { productData, otherData };
